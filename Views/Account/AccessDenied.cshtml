
@{
    ViewData["Title"] = "Không có quyền truy cập";
    Layout = "~/Views/Shared/_HomePage.cshtml";
}

<style>
    .access-denied-container {
        min-height: 80vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
    }

    .access-denied-card {
        max-width: 500px;
        text-align: center;
        padding: 40px;
        background: white;
        border-radius: 15px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    }

    .denied-icon {
        font-size: 80px;
        color: #dc3545;
        margin-bottom: 20px;
    }

    .denied-title {
        font-size: 32px;
        font-weight: bold;
        color: #333;
        margin-bottom: 10px;
    }

    .denied-message {
        color: #666;
        margin-bottom: 30px;
    }

    .btn-group-custom {
        display: flex;
        gap: 15px;
        justify-content: center;
        flex-wrap: wrap;
    }
</style>

<div class="access-denied-container">
    <div class="access-denied-card">
        <i class="bi bi-shield-x denied-icon"></i>
        <h1 class="denied-title">403 - Truy cập bị từ chối</h1>
        <p class="denied-message">
            Xin lỗi, bạn không có quyền truy cập vào trang này.<br>
            Vui lòng liên hệ quản trị viên nếu bạn cho rằng đây là lỗi.
        </p>

        <div class="btn-group-custom">
            <a href="/" class="btn btn-primary">
                <i class="bi bi-house"></i> Trang chủ
            </a>
            <a href="javascript:history.back()" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Quay lại
            </a>
            @if (User.Identity.IsAuthenticated)
            {
                <a href="/Account/Profile" class="btn btn-info">
                    <i class="bi bi-person"></i> Tài khoản
                </a>
            }
            else
            {
                <a href="/Account/Login" class="btn btn-success">
                    <i class="bi bi-box-arrow-in-right"></i> Đăng nhập
                </a>
            }
        </div>

        @if (User.Identity.IsAuthenticated)
        {
            <div class="mt-4 text-muted small">
                Tài khoản hiện tại: <strong>@User.Identity.Name</strong><br>
                Vai trò: <strong>@User.FindFirst(System.Security.Claims.ClaimTypes.Role)?.Value</strong>
            </div>
        }
    </div>
</div>
